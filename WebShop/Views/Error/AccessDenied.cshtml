@{
    ViewBag.Title = "Доступ запрещён";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container text-center mt-5">
    <h1 class="text-danger">Доступ запрещён</h1>
    <p class="lead">У вас нет прав для просмотра этой страницы.</p>
    <a href="@Url.Action("MainPage", "Home")" class="btn--main btn">На главную</a>
</div>